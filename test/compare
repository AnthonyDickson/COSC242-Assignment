if [ -z "$1" ] || [ -z "$2" ] || [ -z "$3" ]; then
	echo "Usage: $0 <test_name_1> <test_name_2> <test_input_file>"
	exit
fi
echo "running test $1..."
test/run $1 < $3
echo "running test $2..."
test/run $2 < $3
echo "diffing tests $1 and $2..."
test/diff $1 $2

